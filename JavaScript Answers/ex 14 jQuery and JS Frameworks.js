// jQuery required

$('#registerBtn').click(() => {
  alert('Thank you for registering via jQuery!');
});

$('.event-card').fadeIn(500).delay(1000).fadeOut(500);

// Benefit of frameworks: React or Vue enable building reusable UI components with state management and virtual DOM for high performance.
